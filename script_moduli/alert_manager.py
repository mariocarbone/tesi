import json

class AlertManager:

    def process_alert(self, alert):
        # Processa l'alert in formato JSON e prende decisioni in base ai valori di distanza
        # Implementa la logica per determinare se arrestare il veicolo, rallentarlo, ecc.

    def receive_alerts(self):
        # Riceve gli alert da qualche sorgente (es. MQTT) e li processa utilizzando il metodo process_alert
